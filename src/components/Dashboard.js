import React from 'react';
import PropTypes from 'prop-types';

const Dashboard = ({ ratings }) => {
  return <div className="main-component">Dashboard...</div>;
};

Dashboard.propTypes = {
  ratings: PropTypes.array,
};

export default Dashboard;
